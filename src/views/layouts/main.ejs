<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= layout.title %></title>

    <link rel="stylesheet" href="/static/css/tailwind.css" />

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Funnel+Display:wght@300..800&family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap"
      rel="stylesheet"
    />

    <!-- CDNJS for JQuery -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
  </head>

  <body class="font-inter text-pearl min-h-screen justify-stretch bg-zinc-900">
    <!-- The navigation bar, this has to be collapsible on mobile. But it doesn't have to be collapsible
     on desktop. -->
    <aside
      id="navigation-bar"
      class="absolute inset-0 z-50 flex -translate-x-full flex-col items-stretch bg-zinc-950 px-6 py-16 duration-200 lg:w-64"
    >
      <button id="navigation-bar-close" class="absolute top-6 right-6">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 -960 960 960"
          class="fill-pearl size-6 duration-200 hover:rotate-90"
        >
          <path
            d="m256-200-56-56 224-224-224-224 56-56 224 224 224-224 56 56-224 224 224 224-56 56-224-224-224 224Z"
          />
        </svg>
      </button>

      <h1
        class="bg-gradient-to-r from-violet-300 to-fuchsia-300 bg-clip-text text-3xl font-bold text-transparent"
      >
        IT Tools
      </h1>
    </aside>

    <!-- The main content, I want to stretch this around the body. We put in a left absolute margin when
     the nav bar is opened. -->
    <main
      class="absolute inset-0 flex items-center justify-center p-6 py-24 duration-200"
    >
      <button id="navigation-bar-open" class="absolute top-6 left-6">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 -960 960 960"
          class="fill-pearl size-6"
        >
          <path
            d="M120-240v-80h720v80H120Zm0-200v-80h720v80H120Zm0-200v-80h720v80H120Z"
          />
        </svg>
      </button>

      <div class="absolute top-6 right-6 flex items-center">
        <a
          href="/login"
          class="rounded-full border-2 border-violet-300 bg-violet-300 px-4 py-2 font-semibold text-black duration-200 hover:bg-zinc-900 hover:text-violet-300"
          >Login</a
        >
      </div>

      <div class="size-full"><%- include("../pages/" + layout.content) %></div>
    </main>
  </body>

  <script defer>
    // The navigation bar close button always closes the navigation bar tab.
    $("#navigation-bar-close").click(() => {
      $("#navigation-bar").addClass("-translate-x-full");
      $("#navigation-bar-open").removeClass("hidden");
      $("main").removeClass("lg:left-64");
    });

    $("#navigation-bar-open").click(() => {
      $("#navigation-bar-open").addClass("hidden");
      $("#navigation-bar").removeClass("-translate-x-full");
      $("main").addClass("lg:left-64");
    });
  </script>
</html>
